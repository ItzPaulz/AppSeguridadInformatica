import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.seguridad.ciberseguro',
  appName: 'AppMovilSeguridadInformatica',
  webDir: 'build'
};

export default config;
